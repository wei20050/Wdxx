<Window
    x:Class="天域取色器.ColorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ColorView"
    Width="400"
    Height="300"
    AllowsTransparency="True"
    Topmost="True"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="302" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid Name="ColorGrid" />
        <StackPanel Grid.Column="1">
            <Label Content="坐标:" />
            <Label
                Name="LabZb"
                Content="8888,8888"
                Foreground="Red" />
            <Label Content="颜色:" />
            <Label
                Name="LabYs"
                Content="#000000"
                Foreground="Red" />
            <Label
                Name="LabYsx"
                Width="60"
                Height="60"
                Margin="5,0"
                HorizontalAlignment="Left" />
            <Label Content="快捷键:" />
            <Label Content="↑↓←→移动鼠标" />
            <Label Content="F8 暂存选择" />
            <Label Content="Enter/左键 选择" />
            <Label Content="Esc/右键 取消" />
        </StackPanel>
    </Grid>
</Window>
